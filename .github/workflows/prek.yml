name: prek

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  prek:
    name: Run prek hooks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - uses: j178/prek-action@v1
